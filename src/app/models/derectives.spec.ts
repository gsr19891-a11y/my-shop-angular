import { Derectives } from './derectives';

describe('Derectives', () => {
  it('should create an instance', () => {
    const directive = new Derectives();
    expect(directive).toBeTruthy();
  });
});
